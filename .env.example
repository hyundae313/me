# 방문 로그를 서버로 보낼 주소.
# 비워두면 브라우저 localStorage 에만 쌓이고(?track=1 로 확인) 외부로는 아무것도 나가지 않습니다.
#
#   수집기(npm run serve)를 같이 띄울 때  →  /collect
#   수집 서버가 다른 도메인일 때          →  https://api.example.com/collect
#
# ⚠️ 이 값은 빌드 시점에 코드에 박힙니다. 바꾼 뒤에는 npm run build 를 다시 하세요.
VITE_TRACK_ENDPOINT=
